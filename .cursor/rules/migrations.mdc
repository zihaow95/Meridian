---
description: Meridian 数据库迁移不可变与安全规范
globs: backend/**/migrations/**
alwaysApply: false
---

# 迁移安全规范

- 每个提交只包含本功能所需迁移；数据迁移与结构迁移尽量分开提交。
- 禁止修改已经进入共享环境（远端/测试/生产）的历史迁移文件。
- 大表变更先评估锁表与回滚成本；生产回滚不能依赖删除已写入的业务事实。
- 提交前必须 `makemigrations --check` 无漂移；迁移必须能从空库完整执行。
- 迁移、测试与实现应在同一功能提交中完整出现。
- 已进入正式流程的业务数据不物理删除；撤回、失效、Pass、退市、归档用领域状态，不建全局软删除字段。
